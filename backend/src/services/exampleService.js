// Example service methods
const getExampleData = async () => {
  // This would typically interact with a database or external API
  return { data: 'Example service data' };
};

module.exports = {
  getExampleData
}; 